# src/duplicate_check.py

from typing import Tuple

def check_duplicate(email_data: dict, text: str) -> Tuple[bool, str]:
    """
    Stub for duplicates. Real approach might use message IDs, content hashing,
    or text similarity checks.
    """
    return False, ""
